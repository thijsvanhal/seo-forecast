# Streamlit Community Cloud prefers requirements.txt (may use uv for speed).
# Keep this file focused on runtime deps only.

streamlit==1.53.1

# Core data/science stack
numpy==2.4.2
pandas==2.3.3
scipy==1.17.0

# Forecasting
prophet==1.3.0
cmdstanpy==1.0.8

# App features / I/O
plotly==5.24.1
holidays==0.50
vacances-scolaires-france==0.8.1
pyarrow==18.1.0
pyyaml==6.0.3
watchdog==6.0.0

# Streamlit/typer utilities
typer==0.12.5
rich==13.9.4

# Compatibility guardrails (Streamlit docs recommend protobuf>=3.20,<6)
protobuf>=3.20,<6
